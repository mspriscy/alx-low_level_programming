#include "main.h"
/**
* add - adds two integers and returns the result
* @c: int to be added to e
* @e: int to be added to c
* Return: sum of c and e
*/
int add(int c, int e)
{
int s;
s = c + e;
return (s);
}
